import React from 'react';
import { Button } from 'semantic-ui-react'

export const CustButton = (props : any) => {
  return (
      <Button primary>Primary</Button>
    //   <Button onClick={() =>
    //   props.sign == "+" ? props.incrementCounter() : props.decrementCounter(-1)} >
    //   {props.sign}
    // </Button>

  );
}